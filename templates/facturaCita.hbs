<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    .container {
      width: 600px;
      margin: 50px auto;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      padding: 20px;
    }
    h2 {
      color: #333;
      text-align: center;
      margin-bottom: 20px;
      font-size: 26px;
      font-weight: 600;
    }

    /* Encabezado */
    .invoice-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #3E50B4; /* Color de fondo atractivo */
      color: #fff; /* Texto blanco */
      border-radius: 8px 8px 0 0;
      padding: 15px 20px;
      margin-bottom: 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .invoice-header .logo {
      font-size: 24px;
      font-weight: 700;
      text-transform: uppercase;
    }

    .invoice-header .info {
      display: flex;
      flex-direction: column;
      text-align: right;
    }

    .invoice-header .info span {
      margin: 2px 0;
      font-size: 14px;
    }

    .invoice-header .info strong {
      font-weight: bold;
    }

    /* Estilo de la tabla de detalles de la factura */
    .invoice-details {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    .invoice-details th, .invoice-details td {
      padding: 12px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }
    .invoice-details th {
      background-color: #f4f4f4;
      color: #333;
      font-weight: 600;
    }
    .invoice-details td {
      color: #555;
    }

    /* Total */
    .total-table {
      width: 100%;
      margin-top: 20px;
      border-top: 2px solid #f4f4f4;
      padding-top: 20px;
    }
    .total-table td {
      padding: 8px;
      text-align: right;
      font-weight: 600;
    }
    .total-table th {
      text-align: left;
      color: #555;
    }

    /* Estilo del pie */
    .footer {
      text-align: center;
      color: #777;
      font-size: 14px;
      margin-top: 30px;
      border-top: 1px solid #ddd;
      padding-top: 15px;
    }
  </style>
</head>
<body>

<div class="container">
  <!-- Encabezado con detalles de la factura -->
  <div class="invoice-header">
    <div class="logo">
      <span>{{companyName}}</span>
    </div>
    <div class="info">
      <span><strong>Factura N°:</strong> {{invoiceNumber}}</span>
      <span><strong>Fecha de emisión:</strong> {{issueDate}}</span>
    </div>
  </div>

  <!-- Detalles de la factura -->
  <table class="invoice-details">
    <thead>
      <tr>
        <th>Descripción</th>
        <th>Total</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>{{service.name}}</td>
        <td>${{service.price}}</td>
      </tr>

      <!-- Mostrar otros servicios asociados a la cita -->
      {{#each otherServices}}
        <tr>
          <td>{{this.serviceName}}</td>
          <td>${{this.servicePrice}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <!-- Información adicional -->
  <table width="100%">
    <tbody>
      <tr>
        <td><strong>Descripción:</strong> {{description}}</td>
      </tr>
      <tr>
        <td><strong>Descuento:</strong> ${{discount}}</td>
      </tr>
    </tbody>
  </table>

  <!-- Total -->
  <table class="total-table">
    <thead>
      <tr>
        <th>Subtotal:</th>
        <td>${{subtotal}}</td>
      </tr>
      <tr>
        <th>Impuestos:</th>
        <td>${{tax}}</td>
      </tr>
      <tr>
        <th>Total:</th>
        <td>${{total}}</td>
      </tr>
    </thead>
  </table>

  <!-- Pie de la factura -->
  <div class="footer">
    <p>Gracias por su preferencia.</p>
    <p>Para cualquier consulta, no dude en contactarnos.</p>
  </div>
</div>

</body>
</html>
